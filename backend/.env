# Django 配置
SECRET_KEY=django-insecure-0*!&*_v@e#shb)s6^)00)x2=gc=jtw2n!9&2^gb5jc1#z!3z62
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# 数据库配置 - 当前使用 SQLite，后续会切换到 MySQL
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3
# 如需切换到 MySQL，请取消下面的注释并设置相应的值
# DATABASE_ENGINE=django.db.backends.mysql
# DATABASE_NAME=metaprompter
# DATABASE_USER=root
# DATABASE_PASSWORD=password
# DATABASE_HOST=localhost
# DATABASE_PORT=3306

# 语言和时区配置
LANGUAGE_CODE=zh-hans
TIME_ZONE=Asia/Shanghai

# LLM 配置 - OpenAI
# 注意：在实际使用时，请替换为真实的 API 密钥
OPENAI_API_KEY="<Replace with your OpenAI API key>"
OPENAI_API_BASE="https://openrouter.ai/api/v1"
DEFAULT_LLM_MODEL="qwen/qwen3-30b-a3b"